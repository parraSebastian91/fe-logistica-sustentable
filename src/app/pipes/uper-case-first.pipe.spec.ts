import { UperCaseFirstPipe } from './uper-case-first.pipe';

describe('UperCaseFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new UperCaseFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
